import React from 'react';
import { useParams } from 'react-router-dom';

const CampaignDetails = (props) => {
    console.log("CampaignDetails props ", props);
    return (
      <div>
        <h1>Campaign Details</h1>
      </div>
    );
  };

export default CampaignDetails;